---
import userData from '../data/user.json';

const { name, profession, bio } = userData;

const replacements: Array<[RegExp, string]> = [
  [/Palestine/g, '<a href="/palestine" class="country-link">Palestine 🇵🇸</a>'],
  [/Sudan/g, '<a href="/sudan" class="country-link">Sudan 🇸🇩</a>'],
  [/Myanmar/g, '<a href="/myanmar" class="country-link">Myanmar 🇲🇲</a>'],
  [/Ukraine/g, '<a href="/ukraine" class="country-link">Ukraine 🇺🇦</a>'],
  [/the DRC/g, '<a href="/drc" class="country-link">the DRC 🇨🇩</a>'],
];
const bioHtml = replacements.reduce(
  (acc, [pattern, value]) => acc.replace(pattern, value),
  String(bio)
);
---

<header class='profile'>
  <div class='profile-picture-content'>
    <figure class='profile-picture' id='profile-picture'>
      <img
        src='/me.webp'
        alt='Portrait of Timothy Brits'
        width='80'
        height='80'
        loading='eager'
        decoding='sync'
      />
    </figure>
  </div>

  <div class='profile-data'>
    <h1 class='profile-name'>{name}</h1>
    <p class='profile-profession'>{profession}</p>
    <div class='profile-bio'>
      <p class='profile-bio-text' set:html={bioHtml} />
    </div>
  </div>
</header>
