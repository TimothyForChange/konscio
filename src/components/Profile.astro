---
import userData from '../data/user.json';

const { name, profession, bio } = userData;

const bioWithDecorativeFlags = String(bio)
  .replace('Palestine', 'Palestine <span aria-hidden="true">🇵🇸</span>')
  .replace('Ukraine', 'Ukraine <span aria-hidden="true">🇺🇦</span>')
  .replace(
    'the Democratic Republic of the Congo',
    'the Democratic Republic of the Congo <span aria-hidden="true">🇨🇩</span>'
  );
---

<header class='profile slide-up-fade'>
  <div class='profile-picture-content'>
    <figure class='profile-picture' id='profile-picture'>
      <img
        src='/me.png'
        alt='This is me!'
        width='80'
        height='80'
        loading='eager'
        decoding='sync'
      />
    </figure>
  </div>

  <div class='profile-data'>
    <h1 class='profile-name'>{name}</h1>
    <p class='profile-profession'>{profession}</p>
    <div class='profile-bio'>
      <p class='profile-bio-text' set:html={bioWithDecorativeFlags} />
    </div>
  </div>
</header>
