---
import { isString } from 'radash';
import { formatKeyToTitle, formatText } from '../../utils/text.ts';

export interface Properties {
  geography?: Record<string, string>;
}

const { geography } = Astro.props as Properties;
---

{
  geography && Object.keys(geography).length > 0 && (
    <section class='geography-section' aria-labelledby='geography-heading'>
      <h2 id='geography-heading'>Geography</h2>

      {Object.entries(geography)
        .filter(
          ([key, value]) =>
            isString(key) &&
            isString(value) &&
            key.trim() !== '' &&
            value.trim() !== ''
        )
        .map(([key, value]) => (
          <div class='term-group'>
            <h3>{formatKeyToTitle(key)}</h3>
            <p set:html={formatText(value)} />
          </div>
        ))}
    </section>
  )
}

<style>
  .geography-section {
    margin-bottom: var(--section-gap);
  }

  .geography-section h2 {
    font-size: var(--section-heading-size);
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin-bottom: var(--heading-gap);
    border-bottom: var(--section-heading-border);
    padding-bottom: var(--section-heading-padding-bottom);
  }

  .geography-section h3 {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin-bottom: var(--gap);
    margin-top: var(--heading-gap);
  }

  .term-group {
    margin-bottom: calc(var(--heading-gap) * 1.25);
  }

  .term-group:last-child {
    margin-bottom: 0;
  }

  .term-group:first-child h3 {
    margin-top: 0;
  }

  @media (width >= 768px) {
    .geography-section h2 {
      font-size: var(--section-heading-size-lg);
    }
  }
</style>
